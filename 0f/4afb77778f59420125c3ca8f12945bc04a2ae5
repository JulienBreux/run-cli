Task: Implement regions-update Target in Makefile
- Added 'regions-update' target to Makefile.
- Target validates presence of gcloud CLI in PATH.
- Uses pipeline 'gcloud compute regions list | tail -n +2 | awk '\''{print $1}'\'' | sort -u' to extract regions.
- Updates 'internal/run/api/region/region.go' while preserving license header, constants, and comments.
- Formats Go file with 'go fmt'.
- Registered target in .PHONY and included description for 'make help'.
